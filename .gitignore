# build
dist/
.astro/

# dipendenze
node_modules/

# ambiente
.env
.env.production
.vercel

# sistema
.DS_Store
Thumbs.db

# Materiale sorgente fornito dal cliente: centinaia di MB di originali.
# Le derivate ottimizzate finiscono in public/img e sono quelle versionate.
CARTELLA IMMAGINI/
Cataloghi tecnici/
Brand kit/
brand-kit-moioli.zip
# Contiene il timelapse originale: 112 MB, HEVC con audio. Quello che il sito
# serve è public/video/chiavi-in-mano.mp4 — 41", muto, 4,4 MB — ed è versionato.
PROGETTO CHIAVI IN MANO/

# Le derivate delle immagini si rigenerano con `npm run images` dagli
# originali in ../moioli-site/assets, che NON stanno in questo repository.
# Restano però versionate: senza, un deploy da CI non avrebbe le immagini,
# perché gli originali non sono raggiungibili dal server di build.
# public/img/
